// This file centralizes configuration values for the test suite.
// It can include timeouts, API endpoints, feature flags, and environment-specific settings.
// These values can be imported throughout the tests to avoid hardcoding and make maintenance easier.


// # Base URL for tests
// BASE_URL=https://en.wikipedia.org

// # Timeouts (in milliseconds)
// DEFAULT_TIMEOUT=15000

// # API keys or tokens (if you later add API tests)
// WIKIPEDIA_API_KEY=your_api_key_here

// # User credentials (for future login tests)
// TEST_USER_EMAIL=testuser@example.com
// TEST_USER_PASSWORD=supersecretpassword

// # Environment flag
// NODE_ENV=development
